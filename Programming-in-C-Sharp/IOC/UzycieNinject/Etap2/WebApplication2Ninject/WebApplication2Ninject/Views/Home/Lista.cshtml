@model IEnumerable<WebApplication2Ninject.Models.Product>

@{
    ViewBag.Title = "Lista";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ProductID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ProductID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ProductID })
        </td>
    </tr>
}
    @{
        foreach (var mojItem in Model)
        {
            if (mojItem.ProductID == 1) {
                var wskazanyProdukt = mojItem;
            }
        }
        var checkedPassword = Model.FirstOrDefault<WebApplication2Ninject.Models.Product>(c=>c.ProductID==2);
        //var checkedPassword = Model.Where(x => x.ProductID == 1);
        var whatIHaveGot = checkedPassword.Name;
    }
<br />
Otrzymalem: 
<p> @whatIHaveGot </p>
</table>
