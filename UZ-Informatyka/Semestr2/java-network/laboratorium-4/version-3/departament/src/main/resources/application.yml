spring:
profiles:
   active: default
   datasource:
          driverClassName: org.h2.Driver
          url: jdbc:h2:tcp://localdhost/~/msaw;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
          username: msaw
          password: msaw
   jpa:
    hibernate:
      ddl-auto: create-drop
      globally_quoted_identifiers: true
   h2:
    console:
      enabled: true
      path: /h2-console